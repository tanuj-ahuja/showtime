    <%= form_for(current_user.movie_relations.build,:remote => true )do |f|%>
 
  <div><%= hidden_field_tag :movie_id, @md[0].movie_id %></div>
    <div><%= hidden_field_tag :movie_title, @md[0].movie_title %></div>

  <%= f.submit "Watched", class: "btn btn-primary"%>
  <%end%>
